import {authService} from '../services/services';import {PageHeader} from '../components/ui';export function Settings(){const u=authService.current();return <div className="page"><PageHeader eyebrow="PROFILE & PREFERENCES" title="Operational profile"/><section className="panel profile"><div className="avatar large">{u?.name.slice(0,1)}</div><div><h2>{u?.name}</h2><p>{u?.role}</p><p>{u?.email}</p></div></section><section className="panel preferences"><p className="eyebrow">NOTIFICATION PREFERENCES</p><h2>Response settings</h2><label><input type="checkbox" defaultChecked/> Receive critical alert notifications</label><label><input type="checkbox" defaultChecked/> Include predicted time windows in summaries</label></section></div>}
