import {NavLink,Outlet,useNavigate} from 'react-router-dom'; import {Bell,LayoutDashboard,Map,Settings,Shield,BriefcaseBusiness,LogOut} from 'lucide-react'; import {authService} from '../services/services';
const nav=[['/dashboard','Dashboard',LayoutDashboard],['/heatmap','Risk Heatmap',Map],['/alerts','Alerts',Bell],['/investigations/CYB-2026-1024','Investigations',BriefcaseBusiness],['/settings','Profile & Settings',Settings]] as const;
export function DashboardLayout(){const u=authService.current();const navigate=useNavigate();return <div className="shell"><aside className="sidebar"><div className="brand"><Shield/><span>CYBER<span>SENTINEL</span></span></div><p className="system-tag">PREDICTIVE INTELLIGENCE</p><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to}><Icon size={18}/>{label}</NavLink>)}</nav><div className="user-card"><div className="avatar">{u?.name.slice(0,1)}</div><div><b>{u?.name}</b><small>{u?.role}</small></div></div><button className="logout" onClick={()=>{authService.logout();navigate('/login')}}><LogOut size={17}/> Sign out</button></aside><main className="main-content"><div className="topbar"><span className="live"><span className="live-dot" aria-hidden="true"></span>LIVE THREAT TELEMETRY</span><button className="topbar-signout" onClick={()=>{authService.logout();navigate('/login')}}><LogOut size={15}/> Sign Out</button></div><Outlet/></main></div>}

