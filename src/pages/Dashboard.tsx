import {useEffect,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {AlertTriangle,Banknote,MapPinned,MessageSquare} from 'lucide-react'; import {Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts'; import {dashboardService,predictionService,alertService} from '../services/services'; import type {Alert,DashboardSummary,Prediction} from '../types'; import {trend} from '../mocks/data'; import {MapView} from '../components/MapView'; import {Loading,PageHeader,RiskBadge,StatusBadge} from '../components/ui';
const icon=[MessageSquare,MapPinned,AlertTriangle,Banknote];export function Dashboard(){const [s,setS]=useState<DashboardSummary>();const [p,setP]=useState<Prediction[]>([]);const [a,setA]=useState<Alert[]>([]);const nav=useNavigate();useEffect(()=>{Promise.all([dashboardService.getSummary(),predictionService.list(),alertService.list()]).then(([x,y,z])=>{setS(x);setP(y);setA(z)})},[]);if(!s)return <Loading/>;const cards=[['Total Complaints',s.totalComplaints,'Validated & linked complaints'],['High-Risk Zones',s.highRiskZones,'Critical and high priority'],['Active Alerts',s.activeAlerts,'Awaiting operational action'],['At-Risk ATMs',s.atRiskAtms,'Next 24-hour forecast']];return <div className="page"><PageHeader eyebrow="OPERATIONAL OVERVIEW" title="Threat picture"><button className="btn secondary" onClick={()=>nav('/heatmap')}>Open full heatmap</button></PageHeader><div className="kpis">{cards.map(([l,v,d],i)=>{const Icon=icon[i];return <article className="kpi" key={l}><Icon/><p>{l}</p><strong>{Number(v).toLocaleString()}</strong><small>{d}</small></article>})}</div><div className="dashboard-grid"><section className="panel map-panel"><div className="section-title"><div><p className="eyebrow">GIS RISK OVERLAY</p><h2>Risk heatmap preview</h2></div><span className="data-note">Demo coordinates • India</span></div><MapView compact data={p.slice(0,5)} onSelect={x=>nav(`/predictions/${x.id}`)}/></section><section className="panel"><div className="section-title"><div><p className="eyebrow">7-DAY SIGNAL</p><h2>Risk trend</h2></div></div><div className="chart"><ResponsiveContainer width="100%" height="100%"><LineChart data={trend}><XAxis dataKey="day" stroke="#8090a8"/><YAxis domain={[0,100]} stroke="#8090a8"/><Tooltip/><Line type="monotone" dataKey="risk" stroke="#4bb3fd" strokeWidth={3}/></LineChart></ResponsiveContainer></div></section><section className="panel wide"><div className="section-title"><div><p className="eyebrow">PRIORITISED HOTSPOTS</p><h2>Top predicted locations</h2></div><button className="link-btn" onClick={()=>nav('/heatmap')}>View all locations</button></div><div className="location-list">{p.slice(0,3).map(x=><button key={x.id} onClick={()=>nav(`/predictions/${x.id}`)}><span className="rank">{x.rank}</span><span><b>{x.location_id}</b><small>{x.location_name} · {x.region}</small></span><strong>{x.risk_score}/100</strong><RiskBadge level={x.risk_level}/></button>)}</div></section><section className="panel"><div className="section-title"><div><p className="eyebrow">RESPONSE QUEUE</p><h2>Recent alerts</h2></div><button className="link-btn" onClick={()=>nav('/alerts')}>Open queue</button></div><div className="alert-list">{a.slice(0,3).map(x=>{const pp=p.find(q=>q.id===x.prediction_id);return pp&&<button key={x.id} onClick={()=>nav('/alerts')}><RiskBadge level={x.severity}/><span><b>{pp.location_id}</b><small>{pp.predicted_window} · {pp.risk_score}/100</small></span><StatusBadge status={x.status}/></button>})}</div></section></div></div>}
