import type {ReactNode} from 'react'; import {AlertTriangle,LoaderCircle,ShieldCheck} from 'lucide-react'; import {riskClass} from '../utils/risk'; import type {RiskLevel} from '../types';
export function RiskBadge({level}:{level:RiskLevel}){return <span className={'badge '+riskClass(level)}>{level}</span>}; export function StatusBadge({status}:{status:string}){return <span className={'status '+(status==='NEW'?'new':'ack')}>{status}</span>};
export function PageHeader({eyebrow,title,children}:{eyebrow:string;title:string;children?:ReactNode}){return <header className="page-header"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1></div><div className="header-actions">{children}</div></header>};
export function Loading(){return <div className="state"><LoaderCircle className="spin"/> Loading intelligence…</div>}; export function Empty({children}:{children:ReactNode}){return <div className="state"><ShieldCheck/> {children}</div>}; export function ErrorState({children}:{children:ReactNode}){return <div className="state error"><AlertTriangle/> {children}</div>};
export function ConfirmModal({open,onClose,onConfirm}:{open:boolean;onClose:()=>void;onConfirm:()=>void}){if(!open)return null;return <div className="modal-backdrop" role="presentation"><section className="modal" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><p className="eyebrow">OPERATIONAL CONFIRMATION</p><h2 id="confirm-title">Acknowledge this alert?</h2><p>This records your acknowledgement and updates the shared operational queue.</p><div className="modal-actions"><button className="btn secondary" onClick={onClose}>Cancel</button><button className="btn" onClick={onConfirm}>Confirm acknowledgement</button></div></section></div>}
